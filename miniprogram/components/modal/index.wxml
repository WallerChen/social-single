<view wx:if="{{showModal}}" class="dialog" bind:tap="closeDialog">
     <view class="dialog-layout" style="{{'top:' + top + 'rpx'}}" >
      <view class="content">
        <slot></slot>
      </view>
     </view>
</view>